operator:
  enable: true
  imagePullPolicy: Always
  image: digizuite.azurecr.io/digizuite/postgres-topology-operator:master
  imagePullSecret: "regcreds"
  resources:
    limits:
      cpu: 100m
      memory: 200Mi
    requests:
      cpu: 100m
      memory: 200Mi
  nodeSelector: {}
